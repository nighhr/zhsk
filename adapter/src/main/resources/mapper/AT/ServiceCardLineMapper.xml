<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhonghe.adapter.mapper.AT.ServiceCardEntryMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT IGNORE INTO at_service_card_line (
        ID, FID, FEntryID, FCardTypeID, FCardTypeName,
        FPrice, FQty, FTotalPrice
        ) VALUES
        <foreach collection="list" item="entry" separator=",">
            (
            #{entry.ID}, #{entry.FID}, #{entry.FEntryID}, #{entry.FCardTypeID}, #{entry.FCardTypeName},
            #{entry.FPrice}, #{entry.FQty}, #{entry.FTotalPrice}
            )
        </foreach>
    </insert>

</mapper>